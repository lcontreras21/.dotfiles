services:
  dotfiles:
    build:
      context: ../
      dockerfile: bootstraps/pop_os.Dockerfile
    container_name: dotfiles
    user: lcontreras21
    tty: true
    entrypoint: /bin/zsh
    environment:
      - TERM=xterm-256color
